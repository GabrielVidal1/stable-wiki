---
sidebar_position: 0
---

# Prompt

<Flow />

The prompt is the main way to control the generated image.

The prompt is then tokenized and fed to the text encoder.
The result is a text embedding that is used to condition the diffuser when generating the image.

The prompt has a maximum length of 77 tokens
